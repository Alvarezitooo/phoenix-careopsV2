# Core web framework (FastAPI migration) - FORCE REBUILD
fastapi==0.115.0
uvicorn[standard]==0.32.0  # ASGI server avec websockets
pydantic==2.9.2  # Validation automatique
pydantic-settings==2.6.0  # Config management

# Legacy Flask (à supprimer après migration complète)
Flask==3.0.0
gunicorn>=23.0.0  # 🔒 Fix: Request smuggling CVE (was 21.2.0)
flask-cors>=5.0.0  # 🔒 Fix: Access-Control-Allow-Private-Network (was 4.0.0)

# Gemini AI
google-generativeai==0.8.3

# Database
supabase>=2.0.0

# Utilities
requests==2.32.4  # 🔒 Fix CVE-2024-47081 & CVE-2024-35195
python-dotenv==1.0.0
tenacity==8.2.3

# Document processing
pypdf>=5.1.0  # 🔒 Fix: Infinite loop CVE + PyPDF2 deprecated → pypdf

# Image processing (pour Gemini Vision)
Pillow>=11.0.0

# Stripe payments
stripe==7.0.0
